in raycastscript toevoegen functie:

//collision detection

public bool getWall( Vector3 start, Vector3 richting)
	{

	GameObject firstCollidedWall = null;
	
	RaycastHit hit;
		
	bool muur_gevonden = false;

	start.y = 1.0F;
	RaycastHit[] hits = Physics.RaycastAll(start, richting, 1.0F);

	for (var i = 0;i < hits.Length; i++) {
		
		hit = hits[i];
			
		firstCollidedWall = hit.collider.gameObject;

		if(firstCollidedWall.tag == "muur")
				
			muur_gevonden =true;
		
	}
		
		
	return muur_gevonden;
	
}

in wiicontroler script
In de functies: moveCameraLeft, moveCameraRight, moveCameraForward, moveCameraBackward
Na de toekenning van cameraRelative en voor cameraRelative.y = 0

bool collision = raycastscript.getWall(playerCam.transform.position, cameraRelative);
if(!collision){